# 2026-06-02

## 项目全面审查与修复

### 执行审查
对 HanziMaster v2.2.0 进行了全面代码审查，发现 12 个问题（3 个严重、4 个中等、5 个低优先级）。

### 已修复的问题

**P0（严重）：**
- 重写 README_EN.md：修正了错误的 Angular 21 描述，更新为实际技术栈（Next.js 15 + React 19）
- 补齐 9 个翻译文件的缺失键：es, ar, fr, de, ja, ko, pt-BR, ru, zh-TW（新增 common.learners, common.strokeMastery, home.featuresTitle, home.featuresSubtitle, practice.* 全部命名空间, learn.radical, learn.structure）
- 修正 DEPLOYMENT_GUIDE.md：输出目录从 `dist/hanzi-master` 改为 `.next`，修正了部署配置

**P1（中等）：**
- 删除 `.eslintrc.json`（与 eslint.config.js flat config 重复）
- 统一 Node.js 版本要求：`.nvmrc` 从 24.5.0 改为 20.11.0，openspec/01-overview.md 同步更新
- 重构 package.json 依赖分类：将 typescript, eslint, postcss, autoprefixer, tailwindcss, @types/*, globals, @typescript-eslint/* 移入 devDependencies

**P2（低）：**
- 删除未使用的 `tailwind.config.ts`（Tailwind v4 使用 CSS @theme 配置）
- 删除未引用的 `metadata.json`
- 为 9 个缺少文件头的源文件添加了编码规范要求的头注释
- openspec/01-overview.md 移除 tailwind.config.ts 引用

### 保留未改动
- @google/generative-ai SDK：计划用于 AI 功能，保留在 dependencies 中
- test/ 目录：保持为空，待后续添加测试
- practice 页面：功能为占位 UI，待后续开发

### 审查发现的系统环境问题
- 当前 Node.js 为 v25.2.1，npm 全局安装有模块缺失问题（@npmcli/config not found），无法运行 `npm run build`，但项目代码本身无问题（TypeScript/Lint 全部通过）

## UI/UX 专业审查与优化 (v2.2.1)

### 审查范围
对全部前端 UI 进行了专业 UI/UX 审查，涵盖视觉层级、交互逻辑、信息架构和响应式适配。
生成了 `UI_UX_REVIEW.md` 详细审查报告（综合评分 3.3/5.0）。

### 修复的严重问题
- **主题切换丢失 system 模式**：改为 3 态循环（light → dark → system），增加 tooltip
- **语言下拉框无障碍缺失**：添加完整 ARIA 属性（listbox/option/aria-expanded/aria-selected）、键盘导航（Arrow/Escape）、溢出滚动

### 修复的中等问题
- 移动端抽屉去重：移除重复的 theme/locale toggle 和无功能的 Sign In 按钮
- 移动端导航移除 emoji（🏠📖✏️），保持专业风格
- 首页 CTA 差异化：开始学习 → /learn，浏览字库 → /practice
- Status Badge 文本修正为 "AI-Powered Learning"
- 字符网格点击不再取消选择（反直觉行为修复）
- Practice 页面响应式间距统一为标准 `px-4 sm:px-6`
- Practice 页增加 tablet 断点 `sm:grid-cols-2`
- 周进度网格移动端适配（gap-1 → sm:gap-3 → lg:gap-4）
- 页脚移除死链接 About/Contact, 桌面 Header 移除 Sign In 按钮

### 优化项
- 添加页面过渡动画（`animate-fade-in-up` + `key={pathname}`）
- `font-display: swap` for Inter 和 Noto Sans SC
- 创建 `container-page` utility class
- Hero 标题最大字号 `text-7xl` → `text-6xl`（更合理的比例）
- Practice 卡片增加 `active:scale-[0.98]` 按压反馈
- 所有响应式断点统一：`px-4 sm:px-6`, `gap-2.5/3 sm:gap-4/6 lg:gap-5/8`

### 变更文件清单
- 修改：globals.css, layout.tsx, page.tsx, learn/page.tsx, practice/page.tsx, theme-toggle.tsx, locale-toggle.tsx, CHANGELOG.md
- 新增：UI_UX_REVIEW.md

## 第二轮修复 (v2.2.1 补充) - 修复所有剩余问题

### 本轮修复项（8 个问题）
1. **桌面 Header Sign In 按钮移除**：前次 CHANGELOG 声称已移除但实际未执行，现真正移除 layout.tsx 第 187-191 行
2. **Practice 卡片反直觉切换**：与 Learn 页相同问题，再点击取消选中 → 改为仅选中不取消
3. **Practice 选中态填充色**：选中卡片添加 tinted 背景色（emerald-50/blue-50/purple-50）和 ring-2 视觉反馈
4. **暗色模式 focus ring 对比度**：`.dark *:focus-visible` 使用更亮的 `#34d399` 替代 `#10b981`
5. **Footer 暗色分隔线对比度**：`border-slate-700/50` → `border-slate-600/60`
6. **字符网格 loading skeleton**：Learn 页新增骨架屏（12 个占位卡片 + shimmer 动画 + `role="status"` 无障碍）
7. **TypeScript 类型安全**：移除 locale-toggle 中的 `as any`，导入 `Locale` 类型
8. **版本号更新**：移动端抽屉 "v2.2.0" → "v2.2.1"
9. **Hero 描述大屏优化**：`max-w-lg` → `max-w-lg lg:max-w-xl`（1440px+ 屏幕更合理空间利用）

### 变更文件
- layout.tsx（移除 Sign In、版本号、foot 分隔线）
- practice/page.tsx（取消反切换、选中填充色）
- learn/page.tsx（loading skeleton）
- globals.css（暗色 focus ring）
- locale-toggle.tsx（Locale 类型）
- CHANGELOG.md（更新日志）
- page.tsx（hero 描述大屏宽度）

## 第三轮修复 (v2.2.1 最终) - 全面代码审查修复

### 发现 29 个问题，修复全部可修复项

### Bug 修复
1. **`@tailwindcss/vite` → `@tailwindcss/postcss`**：Vite 插件不兼容 Next.js PostCSS 管线
2. **`images.domains` → `images.remotePatterns`**：Next.js 15+ 废弃 API，更新 next.config.js
3. **CSS `::focus` 语法**：已验证为正确 `:focus`（单冒号），无需修改

### 版本同步（全部统一为 v2.2.1）
- package.json: 2.2.0 → 2.2.1
- 所有源文件头注释（10 个文件）：v2.2.0 → v2.2.1
- 所有翻译文件头注释 + footer 版权（11 语言 × 2）：v2.2.0 → v2.2.1

### 类型安全
- locale-provider.tsx: `getNestedValue(obj: any)` → `obj: Translations`
- practice/page.tsx: `getIcon(..., colorClasses: any)` → `Record<string, string>`

### 死代码清理
- 从 11 个翻译文件移除 `common.signIn` 键（对应按钮已删除）

### 功能增强
- 新增 `common.theme.*` 翻译键（light/dark/system），支持 11 种语言
- theme-toggle tooltip 从硬编码英文改为 i18n 翻译

### Learn 页优化
- 移除 300ms 模拟加载（骨架屏仅保留 CSS，不再有假 loading 状态）
- 页面即时渲染

### 变更文件
- package.json, next.config.js
- globals.css, layout.tsx, page.tsx, learn/page.tsx, practice/page.tsx
- theme-provider.tsx, theme-toggle.tsx, locale-provider.tsx, locale-toggle.tsx
- i18n/index.ts
- 11 个翻译文件（en, zh-CN, zh-TW, es, ar, fr, pt-BR, de, ja, ko, ru）
- CHANGELOG.md
